pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.{js,jsx,ts,tsx,json,css,html,yml,yaml,md}"
      run: npx oxfmt {staged_files}
      stage_fixed: true

    - name: lint
      glob: "*.{js,jsx,ts,tsx}"
      run: npx oxlint {staged_files}
